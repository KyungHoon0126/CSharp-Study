<Page
    x:Class="CustomControl.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CustomControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="115*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="CustomControl Sample" 
                   Style="{StaticResource TitleTextBlockStyle}"/>

        <StackPanel Grid.Row="1"
                    VerticalAlignment="Center" 
                    Margin="20,235,20,300">
            
            <local:SimpleSlider x:Name="simpleSlider" 
                                Height="20" 
                                Maximum="100"/>
            <TextBlock Text="{Binding Value, ElementName=simpleSlider}"/>
        </StackPanel>
    </Grid>
</Page>
